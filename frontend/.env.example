# Vite dev proxy target for the Node backend.
VITE_BACKEND_ORIGIN=http://127.0.0.1:8080

# Request the backend directly instead of silently depending on the Vite /api proxy.
VITE_API_BASE_URL=http://127.0.0.1:8080

VITE_API_TIMEOUT_MS=6000
VITE_USE_MOCK=false
VITE_API_FALLBACK_TO_MOCK=false
